Loading data
============

Apps with the type ``"loader"`` will load data from file(s). When composed into a process, loaders can be applied in batch to multiple data files. 

.. toctree::
    :maxdepth: 1

    load-aligned
    load-unaligned
    load-tabular
    load-json